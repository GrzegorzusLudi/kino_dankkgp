{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "youtube-player",
  "prefix": "lib",
  "projectType": "library",
  "sourceRoot": "libs/youtube-player/src",
  "tags": [],
  "targets": {
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run youtube-player:lint:fix",
          "nx run youtube-player:lint:styles:fix",
          "nx run youtube-player:format"
        ],
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/youtube-player/\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    },
    "lint:styles": {
      "executor": "nx:run-commands",
      "options": {
        "command": "stylelint \"libs/youtube-player/**/*.scss\" --allow-empty-input"
      }
    },
    "lint:styles:fix": {
      "executor": "nx:run-commands",
      "options": {
        "command": "stylelint \"libs/youtube-player/**/*.scss\" --allow-empty-input --fix"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "libs/youtube-player/vite.config.mts"
      }
    }
  }
}
