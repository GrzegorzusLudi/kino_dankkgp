{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "footer",
  "prefix": "lib",
  "projectType": "library",
  "sourceRoot": "libs/footer/src",
  "tags": [],
  "targets": {
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run footer:lint:fix",
          "nx run footer:lint:styles:fix",
          "nx run footer:format"
        ],
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/footer/\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    },
    "lint:styles": {
      "executor": "nx:run-commands",
      "options": {
        "command": "stylelint \"libs/footer/**/*.scss\" --allow-empty-input"
      }
    },
    "lint:styles:fix": {
      "executor": "nx:run-commands",
      "options": {
        "command": "stylelint \"libs/footer/**/*.scss\" --allow-empty-input --fix"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "libs/footer/vite.config.mts"
      }
    }
  }
}
