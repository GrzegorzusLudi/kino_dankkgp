{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "title",
  "prefix": "lib",
  "projectType": "library",
  "sourceRoot": "libs/title/src",
  "tags": [],
  "targets": {
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run title:lint:fix", "nx run title:format"],
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/title/\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "libs/title/vite.config.mts"
      }
    }
  }
}
