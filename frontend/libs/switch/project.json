{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "switch",
  "prefix": "lib",
  "projectType": "library",
  "sourceRoot": "libs/switch/src",
  "tags": [],
  "targets": {
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run switch:lint:fix",
          "nx run switch:lint:styles:fix",
          "nx run switch:format"
        ],
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/switch/\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    },
    "lint:styles": {
      "executor": "nx:run-commands",
      "options": {
        "command": "stylelint \"libs/switch/**/*.scss\" --allow-empty-input"
      }
    },
    "lint:styles:fix": {
      "executor": "nx:run-commands",
      "options": {
        "command": "stylelint \"libs/switch/**/*.scss\" --allow-empty-input --fix"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "libs/switch/vite.config.mts"
      }
    }
  }
}
