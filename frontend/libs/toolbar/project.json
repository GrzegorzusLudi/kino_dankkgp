{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "toolbar",
  "prefix": "lib",
  "projectType": "library",
  "sourceRoot": "libs/toolbar/src",
  "tags": [],
  "targets": {
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run toolbar:lint:fix", "nx run toolbar:format"],
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/toolbar/\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "libs/toolbar/vite.config.mts"
      }
    }
  }
}
